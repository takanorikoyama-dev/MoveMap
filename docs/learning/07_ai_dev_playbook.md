# 📖 AI 駆動開発プレイブック

> 本プロジェクトで実践した **twin-build フロー** + **AI 共同開発** の知見を、
> **次の案件で再利用できる形** に汎用化したプレイブック。
> セールスエンジニア / AI コンサル目線で「クライアントに同じやり方で進められるか」を判断する材料にも。

---

## 1. AI 駆動開発とは(1 行)

> **「人間が判断、AI が生成・実装」を、フェーズゲートで構造化した開発スタイル**

| 要素 | 従来開発 | AI 駆動開発 |
|---|---|---|
| 仕様策定 | 人間が書く | **AI が対話で引き出し、人間が判断** |
| コード生成 | 人間が書く | **AI が生成、人間がレビュー** |
| 設計整合性 | 人間が頑張る | **Coverage で機械的に検証** |
| フェーズ移行 | 暗黙的 | **Gate で意思決定を記録** |
| 速度 | 人間の手作業に依存 | **数倍〜10 倍速** |

---

## 2. AI 駆動開発の 5 原則(MoveMap プロジェクトから抽出)

### 🟢 原則 1: 「フェーズゲートを守る」

各フェーズの成果物が前フェーズに整合していることを確認してから次に進む。

**実例**: Phase 0(課題) → Phase 1(要求) → ... → Phase 8(レビュー)
- Phase 1 を始める前に Phase 0 の「課題」が明確か確認
- Coverage(設計と実装の整合性メトリクス)を可視化

**やってはいけないこと**:
- フェーズを飛ばす(例: 課題定義なしで設計に入る)
- AI が生成した設計を読まずに次フェーズへ

### 🟢 原則 2: 「AI に任せること / 人間が判断することを分ける」

| AI に任せる | 人間が判断 |
|---|---|
| コード生成 | 技術スタック選定 |
| ドキュメント作成 | プロダクト戦略 |
| テスト書く | ペルソナ定義 |
| ライブラリ調査 | 課金プラン設計 |
| エラーメッセージ解読 | セキュリティ判断 |
| パターン適用 | データソース信頼性評価 |
| 単純なリファクタ | 競合差別化 |
| 命名提案 | API キーの貼付 / 公開判断 |

**実例**:
- Streamlit を選んだのは人間判断、各 Usecase の実装は AI
- reinfolib API がダメだと分かった時の「賃料ソース切り替え」は人間判断、新アダプタの実装は AI

### 🟢 原則 3: 「1 件 probe してから本実装」

外部 API / ライブラリ / フレームワーク を使う時、**まず 1 件叩いてレスポンス・挙動を確認** してからコード化する。

**実例**:
- e-Stat の `0003411634` を「都道府県別出生数」と想定 → 1 件 probe で **全国計のみ** と判明 → `0003412062` に変更
- reinfolib `XCT001` を「賃料指数」と想定 → 実は **地価公示** だった → 別 API 探索

**実装パターン**:
```python
# Step 1: 1 件 probe
def probe():
    r = requests.get(URL, params={"id": "test"})
    print(r.json())  # 中身を目で確認

# Step 2: probe 結果を元にパーサ設計
# Step 3: 単体テストでパーサ検証
# Step 4: 結合テストで DB 連携
```

### 🟢 原則 4: 「動くものを 1 周作ってから磨く」

最小限の機能で **End-to-End 動く状態** を最速で作り、そこから改善する。

**実例**:
1. ダミーデータで 47 都道府県 MAP が動く ← まずここ
2. 実 API 連携(1 ソースずつ)
3. UI/UX 改善
4. スマホ対応
5. 課金プラン検討

**やってはいけないこと**:
- 完璧な設計を求めて実装に入らない
- 全機能揃うまでデプロイしない

### 🟢 原則 5: 「透明性を組み込む」

ユーザーが「データが嘘か本当か」を **自分で判断できる材料** を UI に組み込む。

**実例**:
- データ鮮度表示(🔵 DB 実データ / 🟡 ダミー)
- 最終更新日表示
- 出典明記(e-Stat / 国交省 / WAQI)
- AI モデルの R² と学習日時(モデル根拠タブ)
- 予測対象外都道府県の灰色表示 + 件数キャプション

「**信頼は透明性から生まれる**」がこのプロジェクト全体の柱。

---

## 3. Phase 0-8 各フェーズのチェックリスト

### Phase 0: 課題定義(問題発見)

**目的**: 「誰の何の問題を解くか」を 1 文で言える状態にする

**チェックリスト**:
- [ ] ターゲット顧客は誰か?(1 ペルソナ + 補助 1-2 ペルソナ)
- [ ] 顧客の現状の不満 / 不便 / 諦めは何か?
- [ ] その問題が解けると、顧客にどんな価値が生まれるか?
- [ ] 既存の代替手段は何か?(競合 / DIY / 諦め)
- [ ] 問題の規模感(N 万人 / N 億円市場)

**AI への指示テンプレ**:
> 「以下のテーマで、ターゲット顧客 / 痛み / 価値 / 代替手段 / 規模感の 5 項目を整理してください: [テーマ]」

**成果物**: `outputs/00_problem.md`(1-2 ページ)

---

### Phase 1: 要求仕様

**目的**: Must / Should / Could を明確化し、優先度を決める

**チェックリスト**:
- [ ] Must(これがないとプロダクトと言えない)が 5-10 個
- [ ] Should(あれば価値が大きい)が 5-10 個
- [ ] Could(あったら最高、後回し可)が 5-10 個
- [ ] 各要件に **検証可能な完了条件**(Acceptance Criteria)

**AI への指示テンプレ**:
> 「Phase 0 の課題に対し、Must/Should/Could に分けて 15-25 個の要件を出してください。各要件に Acceptance Criteria を 1-3 個付けてください」

**注意点**: AI は「全部 Must」にしがち → 人間が削る

**成果物**: `outputs/01_requirements.md`

---

### Phase 2: 業務構造

**目的**: 登場人物(エンティティ)と関係性を整理

**チェックリスト**:
- [ ] エンティティ一覧(オブジェクト / アクター)
- [ ] エンティティ間の関係(所有 / 参照 / 集約)
- [ ] 各エンティティの主要属性
- [ ] 不変条件(INV)の候補

**成果物**: `outputs/02_business_structure.md` + ER 図(Mermaid)

---

### Phase 3: 業務フロー

**目的**: ユーザーがどう操作するか、データがどう流れるか

**チェックリスト**:
- [ ] ユーザーシナリオ(Happy Path + Error Path)
- [ ] データフロー図
- [ ] 月次 / 日次バッチ等の自動処理フロー
- [ ] 例外処理(外部 API 失敗 / DB 故障 / 認証失敗 等)

**成果物**: `outputs/03_business_flow.md` + シーケンス図

---

### Phase 4: システム要件

**目的**: 非機能要件(性能 / セキュリティ / 可用性 / 保守性)を明確化

**チェックリスト**:
- [ ] 性能(ページロード時間、同時接続数)
- [ ] 可用性(SLA、ダウンタイム許容)
- [ ] セキュリティ(認証、認可、暗号化、監査ログ)
- [ ] スケーラビリティ(ユーザー増加時の対応)
- [ ] 保守性(ログ / モニタリング / バックアップ)
- [ ] **不変条件(INV)を確定**

**成果物**: `outputs/04_system_requirements.md` + `outputs/baseline.md`(INV 一覧)

---

### Phase 5: アーキテクチャ

**目的**: 技術スタック選定と全体構造

**チェックリスト**:
- [ ] 言語 / フレームワーク(理由付き)
- [ ] データストア(理由付き)
- [ ] 外部サービス連携
- [ ] デプロイ環境
- [ ] フォルダ構造(VSA か レイヤーアーキか)
- [ ] **代替案を 1-2 個** 出して比較表で記録

**AI への指示テンプレ**:
> 「以下の要件に対する技術スタック候補を 2-3 個出し、メリット / デメリット / コスト / 学習曲線で比較表を作ってください: [要件サマリ]」

**注意点**: AI は「流行りのスタック」を提案しがち → 人間がチームのスキルセット・運用コストで判断

**成果物**: `outputs/05_architecture.md`

---

### Phase 6: システム設計

**目的**: 詳細設計(画面 / API / データ / 状態遷移 / シーケンス / テスト)

**チェックリスト**:
- [ ] 画面設計(SCR-XXX 番号付き、ワイヤーフレーム)
- [ ] API 設計(エンドポイント、リクエスト / レスポンス)
- [ ] データ設計(テーブル、CHECK 制約、FK)
- [ ] 状態遷移(注文 / バッチ / ユーザーの状態マシン)
- [ ] シーケンス図(主要フロー)
- [ ] テスト設計(TC-XXX 番号付き、Must カバレッジ確認)

**成果物**: `outputs/06_system_design/` 配下に複数ファイル

---

### Phase 7: 実装

**目的**: 設計通りに動くコードを書く + テスト

**チェックリスト**:
- [ ] 各 Usecase の impl が完成
- [ ] 各 Usecase の impl_test が完成
- [ ] Coverage(設計 ↔ 実装 ↔ テスト)が 100% 近い
- [ ] CI(GitHub Actions)が緑
- [ ] **`/review` 評価が B+ 以上**

**AI への指示テンプレ**:
> 「Phase 6 の設計書を読んで、[Usecase 名] の impl と impl_test を生成してください。
> 既存コードの命名・パターンに合わせてください。
> テストは正常系 + 異常系 + エッジケースを各 1-2 個」

**成果物**: `app/` 配下 + `tests/` 配下

---

### Phase 8: レビュー

**目的**: 最終チェック + 振り返り + 次フェーズへの引き継ぎ

**チェックリスト**:
- [ ] **`/review` 評価**(consistency / expert / risk / business)
- [ ] セキュリティチェック(OWASP / 依存脆弱性)
- [ ] パフォーマンステスト
- [ ] 残課題・持ち越しの明確化
- [ ] 次に着手する Top 3 タスク決定

**成果物**: `outputs/99_review.md`

---

## 4. 「AI に任せる」のレベル分け

```
レベル 0: 何もない(全部人間)
レベル 1: AI に質問する(壁打ち)
レベル 2: AI にコード片を書かせる(snippet)
レベル 3: AI に Usecase 単位で書かせる(機能単位)  ← MoveMap 現状
レベル 4: AI に Phase 単位で書かせる(設計 + 実装 + テスト)
レベル 5: AI 完全自律(目標を与えるだけ)
```

**MoveMap の現状**: レベル 3-4 のハイブリッド
- 各 Usecase は AI が生成 → 人間がレビュー
- 設計書も AI が雛形を生成 → 人間が意思決定
- 重要な判断(技術選定 / 課金プラン / セキュリティ)は人間

---

## 5. つまずきポイント TOP 10(MoveMap から)

### #1: API 仕様と実レスポンスの乖離

**例**: reinfolib XCT001 が「不動産価格指数」想定 → 実は地価公示
**対策**: 必ず 1 件 probe してから本実装

### #2: 文字コード問題(Windows cp932)

**例**: `R²` を logger に出力すると UnicodeEncodeError
**対策**: ログ出力は ASCII 化 OR `sys.stdout.reconfigure(encoding="utf-8")`

### #3: API パラメータの型(int vs str)

**例**: reinfolib `area=1`(int)→ 400、`area="01"`(str ゼロパディング)→ 200
**対策**: エラーメッセージを読んで型を疑う

### #4: 外部依存の発行待ち

**例**: reinfolib API キー 5 営業日待ち
**対策**: 並行作業ができるよう設計、フォールバック実装

### #5: ライブラリの非互換変更

**例**: Streamlit `use_container_width` の deprecation
**対策**: 警告は無視せず CHANGELOG を月次確認

### #6: テストが実 API を叩いてしまう

**例**: `__init__(api_key=None)` でも env から実キーを取って実 API を叩く
**対策**: `monkeypatch.delenv` + 明示的 None 上書き

### #7: AI の幻覚(hallucination)

**例**: AI が「e-Stat API には XXX エンドポイントがある」と言うが実在しない
**対策**: AI の出力は **公式ドキュメント or 1 件 probe** で必ず検証

### #8: 設計と実装の乖離

**例**: Phase 6 で「キャッシュする」と決めたが、実装で忘れる
**対策**: Coverage 検証 + Phase 7 完了時の `/review` で機械チェック

### #9: 「ちゃんと動いた」幻想

**例**: テストは通るが、UI で見たら表示崩れ
**対策**: smoke テスト + 必ず手動で UI 確認 + スクリーンショット

### #10: スコープクリープ

**例**: 「ついでに XX も実装しよう」が積み重なって完了しない
**対策**: Must/Should/Could を厳守、Should 以下は別 Phase

---

## 6. AI 駆動開発に必要なツール

| ツール | 用途 | 代替 |
|---|---|---|
| **Claude Code** | AI とのやり取り + ファイル操作 | Cursor, Aider |
| **twin-build** | フェーズ管理 + Coverage | 自前で手順書 |
| **GitHub Actions** | CI/CD | GitLab CI, CircleCI |
| **pytest** | テスト | unittest |
| **uv** | パッケージ管理 | poetry, pip |
| **DuckDB** | ローカル DB | SQLite, PostgreSQL |
| **Streamlit** | プロトタイプ UI | Gradio, Dash |
| **Plotly** | 可視化 | Matplotlib, D3.js |

---

## 7. 次案件で再利用するためのテンプレ集

### A. AI への初回指示テンプレ

```
あなたは [役割] です。
これから [プロジェクト名] の Phase [N] を進めます。

【前提】
- Phase [N-1] の成果物: [リンク or 内容]
- 制約: [予算 / 期限 / 技術スタック]
- 私の役割: 判断・レビュー
- あなたの役割: 提案・生成・実装

【今回のゴール】
[Phase N で達成したいこと]

【進め方】
1. まず質問してください(私の情報不足を埋めるため)
2. 質問が解消したら、案を 2-3 個出してください
3. 私が選んだ案に対して、詳細を実装してください

【出力フォーマット】
- 設計: Markdown
- コード: ファイル単位で、フォルダ構造を含める
- テスト: pytest 形式
```

### B. Phase 完了時のチェックリスト(テンプレ)

```
- [ ] 成果物が `outputs/` または `app/` に保存された
- [ ] AI 生成内容を人間が **1 行ずつ** レビューした
- [ ] Coverage が 80% 以上
- [ ] CI が緑
- [ ] 次 Phase への持ち越し論点が記録された(課題 / TODO / RISK)
- [ ] memory に進捗 + 学びを追記
```

### C. AI コンサル文脈で使うフレーズ集

| 状況 | フレーズ |
|---|---|
| 顧客が AI 駆動開発の経験ゼロ | 「最初の 1 案件は教育投資として、私たちが伴走します」 |
| 「AI に全部任せたい」と言われた | 「判断は人間、生成は AI が原則。例えばこんな判断が必要です…」 |
| 「コードレビューする時間がない」 | 「AI が書いたコードを **全件レビュー** より **抜き取り + テスト** で品質担保します」 |
| 「セキュリティ大丈夫?」 | 「依存脆弱性は pip-audit で CI チェック、API キーは .gitignore + .env で隔離、本番は別管理」 |
| 「コストは?」 | 「Claude API 月 $20-100、開発工数 50% 削減で人件費圧縮、ROI は 3 ヶ月で出る想定」 |

---

## 8. AI 駆動開発を提案する時の「3 つの説得材料」

### 材料 1: スピード

> 「MoveMap は **5 日で 7 タブのアプリ** + AI 予測 + 実 API 連携を完成させました。
> 従来開発なら 2-3 ヶ月かかる規模です」

### 材料 2: 品質

> 「テスト 199 件、Coverage 100%、不変条件 18 件全て検証済み。
> AI に書かせたから雑、ではなく **構造化されているから品質が担保できる** のです」

### 材料 3: 透明性

> 「twin-build フローの Phase 0-8 で **判断の記録** が全部残っています。
> なぜこの技術を選んだか、なぜこの仕様か、後から追えます」

---

## 9. やってはいけないアンチパターン

### ❌ AI の出力を **読まずに** マージする

→ 必ず 1 行ずつ目を通す。少なくとも diff は全件確認。

### ❌ AI に「全部やって」と丸投げ

→ 判断材料(制約 / 目的 / 前提)を渡さないと意味不明な提案が返ってくる。

### ❌ AI を「議論パートナー」として使わない

→ 「これでいいかな?」と聞かず、「3 つ案を出して比較して」と聞く。

### ❌ コードを残さずチャットだけで完結

→ 必ず Git にコミット、判断は memory / outputs/ に記録。

### ❌ 失敗を隠す

→ AI 駆動でもバグは出る。包み隠さず学びとして書き残す(本ドキュメント #5 のように)。

---

## 10. 関連ドキュメント・参考リソース

- [01_executive_summary.md](01_executive_summary.md) — MoveMap 全体観
- [03_architecture.md](03_architecture.md) — VSA とアーキテクチャ
- `outputs/00_problem.md` 〜 `outputs/99_review.md` — twin-build フローの実例
- Claude Code 公式: https://docs.anthropic.com/claude-code
- twin-build: reality-xrcloud-inc/twin-build(社内アクセス)
- VSA 解説: Jimmy Bogard "Vertical Slice Architecture"
- AI 駆動開発: Anthropic "Best practices for using Claude for coding"

---

## 結び: AI 駆動開発の哲学

> **「AI は速い助手、人間は遅い判断者」**
>
> AI に全てを任せてはいけない。
> しかし、判断と方針が決まっているなら、AI は人間の 10 倍速で実装する。
>
> **判断を磨くために時間を使い、実装は AI に任せる。**
> これが AI 時代の開発者の役割。

---

## 付録 A:2026-06 ローンチ期の追加学び(DEC-016 + 計測駆動)

> Phase 0-8 完了後、ローンチ判断・公開準備で得た知見を **後追いで追記**。
> 「設計書だけでは身につかない、現実から方向転換する力」の具体例。

### A-1. 公開判断のための専用 Phase が必要だった

#### 状況
- Phase 8 完了 → "個人ツール限定(DEC-005)" のまま実装は完成
- 2026-05-30、ポートフォリオ用に公開したくなった
- 既存の Phase 0-8 には「公開判断」を扱う場所がない

#### やったこと
- **DEC-016 として新規起票**(DEC-005 を上書き)
- 派生 INV を 3 つ追加(INV-BIZ-006/007/008)
- 法務 3 文書(terms / privacy / cookie consent)を新規整備
- R-03(法的リスク)を再オープンして対応方針を明文化

#### 学び
- twin-build の Phase 0-8 は **作る** ためのフェーズ
- **公開判断は別フェーズ**(P9 相当)として明示的に扱うべき
- DEC は「過去形のログ」だが、**公開判断のような不可逆な意思決定** は GATE → DEC の順で記録すると安全

### A-2. プラットフォーム制約は早期に把握すべき

#### 状況
- Streamlit を「Python の HTML テンプレ」のように使えると想定
- 実装後半で **DOMPurify が `<script>` を除去** することが判明
- `onclick` 属性も除去される → JS-based UI が動かない

#### やったこと
- ヒーローを **`components.html` の iframe** に移行(JS が動く)
- 戻るボタンは **`st.button` + session_state リセット**(HTML link は不安定)
- meta タグは `st.markdown` で注入(`<head>` ではないが Google は body 内 meta も認識)

#### 学び
- **プラットフォーム制約を Phase 5(アーキ)で確認すべきだった**
- 「Streamlit にできること / できないこと」を実 probe で先に押さえる
- Next.js や Hugo で実装した場合との具体的トレードオフを Phase 5 で言語化

### A-3. UX 大幅刷新の手戻りを避ける方法

#### 状況
- Phase 7 でサイドバー前提の UI を実装(指標・時点を左に固定)
- 2026-06-02、「ホームを全画面ワイドビジュアルにしたい」要望
- → サイドバー廃止 + state-based router に **大幅刷新**

#### やったこと
- 6 タブを `st.tabs` から **6 セクションバナー + state router** に移行
- ヒーロー + バナーで「ファストビュー」を作る
- 旧サイドバー機能(指標 / 時点)は各 view 内にインラインで再配置

#### 学び
- **公開時のターゲット(誰に見せるか)が変わると UX 設計が大きく変わる**
- Phase 1(要求)で「**個人用 vs 公開用**」を明確に分けるべき
- 公開用ならファストビュー、ポートフォリオピッチ用、SEO 等を Phase 5 で考慮

### A-4. 「公開して終わり」の罠 — 計測駆動の重要性

#### 上司からのアドバイス(2026-06-05)
> "作る前の企画は強い一方、今後は作った後の企画(計測→学習→改善)を回す"
> 1. 公開前に仮説を 1〜2 個、数値で書く
> 2. GA4 にカスタムイベントを仕込む
> 3. 2 週間後にデータを読み、当たり外れを記録する

#### やる準備をしていること
- 公開前に仮説 2 つを数値で書く(`docs/learning/02_self_quiz.md` F-2 参照)
- GA4 カスタムイベント実装(`inject_ga4` を拡張、URL 変化をトリガに発火)
- T+14 日後の振り返り(カレンダー予約)

#### 学び
- twin-build には Phase 0-8 がある一方、**Phase 9 = 計測 / 学習 / 振り返り** を加える必要
- Phase 8 = 「実装振り返り」だが、計測駆動の Phase 9 = 「**ユーザー反応振り返り**」
- 設計書だけで身につかない「**現実から方向転換する力**」を養うのは Phase 9

### A-5. データ品質は単位定義から始まる

#### 状況
- `public_safety` を「絶対認知件数」で実装
- 公開時に気づいた:「東京 96k 件 vs 秋田 2k 件」=人口と強く相関
- 「東京 = 危険、地方 = 安全」の誤誘導が発生

#### やったこと
- **千人率(件/千人)に正規化**(2026-06-05)
- 全国平均 5〜10 件/千人の現実的レンジに修正
- 定義文を全面書き換え(「人口で正規化済」明記)

#### 学び
- **指標の単位定義は Phase 4(システム要件)で確定すべき**
- 比較可能性を考慮していない単位は **後から必ず正規化が必要**
- 単位選定は「**比較したい軸**」から逆算する(人口当たり / 面積当たり / 時間当たり)

### A-6. AI コスト削減の実体験

#### やったこと
- 47 県の魅力記事生成を AI(Claude API)で実装する案
- コスト試算:有料 API で年 ¥400 程度 → 不要に
- **GitHub Models(完全無料)** に切替:GPT-4o-mini で品質十分

#### 学び
- AI 課金を **「機能が出る」前に試算** すべき(設計時点でコスト試算)
- 無料 / 低コストの代替手段(GitHub Models / Gemini 無料枠 / Groq 等)を**常に把握しておく**
- 課金型 AI と無料 AI のトレードオフは「品質 vs 安定性」(無料は rate limit やサポート無し)

---

## 付録 B:Phase 9「計測駆動振り返り」フレームワーク(新規提案)

### Phase 9 の位置づけ

```
Phase 0-8 (twin-build フロー、作る)
     ↓ 公開判断(DEC)
[ローンチ]
     ↓
[T0]
     ↓ Phase 9-a: 仮説立て(公開前 30 分)
     ↓ Phase 9-b: 計測実装(技術タスク、1-2h)
     ↓ Phase 9-c: 公開 + 動作確認(ユーザー作業)
     ↓
[14 日間の観察期間]
     ↓
[T+14]
     ↓ Phase 9-d: 振り返り(1h)
     ↓ → 次の仮説 + 打ち手の起点
```

### Phase 9 の成果物

| 成果物 | 内容 |
|---|---|
| `outputs/hypotheses/H1.md` 等 | 仮説 1 個ずつ、数値検証可能な形 |
| GA4 カスタムイベント実装 | `view_change` `indicator_change` `horizon_change` `diagnosis_complete` 等 |
| `outputs/reviews/T+14.md` | 14 日後の振り返り、当たり / 外れの記録、次の仮説 |

### Phase 9 と既存 Phase の関係

- **Phase 8(コードレビュー振り返り)** = 「コード品質、設計の妥当性」
- **Phase 9(ユーザー反応振り返り)** = 「仮説と実測のギャップ、ペルソナ妥当性」
- 両方を回すことで「**完全な振り返りループ**」が成立する

---

## 付録 C:AX セールス(技術を売る / 語る)の実体験

> このプロジェクトを通じて、**技術を非エンジニアに説明する** スキルが大幅向上した。
> その実体験を整理。

### C-1. 「30 秒のフック」を最初に作る

- ❌ NG:「Streamlit + DuckDB + Plotly で…」(技術用語先行)
- ⭕ OK:「**全国 47 都道府県を 9 観点で比較・診断・予測できるサイト**を、20 日で個人開発しました」

### C-2. 「数字」と「対比」で説得力を作る

- ❌ NG:「いろいろ実装しました」
- ⭕ OK:「**9 指標 × 47 都道府県 = 423 件**のデータを統合、**232 件のテスト** 全件パス、**¥0/年で運用** 可能」

### C-3. 「失敗」を語れることが信頼を生む

- ❌ NG:「特に問題なく完成しました」
- ⭕ OK:「① 治安指標を絶対件数で実装 → 都市 vs 地方の誤誘導に気づき千人率に修正、② サイドバー前提の UI を公開時に全面刷新、③ Streamlit の HTML サニタイザに悩まされた」

### C-4. 「自分の介在価値」を AI と区別して語る

- ❌ NG:「Claude が全部書いてくれました」(誰でもできる扱い)
- ⭕ OK:「**課題設定 / 設計判断 / 法務判断 / QA は私**、**コード形は AI**。例えば DEC-016 で公開判断する時、法務 3 文書の構成と判断軸は私が決めました」

### C-5. 「コスト」「リスク」「次の打ち手」までセットで語る

- ❌ NG:「完成しました!」
- ⭕ OK:「公開済み(¥0/年)、計測駆動の T+14 仮説検証中、次は SEO 強化のため LP 構築予定」

---

## 結び(追記)

> **「作って終わり、ではなく、計測して学んで方向転換する」**
>
> Phase 0-8 で「作る」を磨いた次は、Phase 9 で「**作った後**」を磨く。
> 設計書では身につかない「現実から方向転換する力」は、
> 公開して、計測して、振り返って、を繰り返すことでしか身につかない。
>
> AI は実装を加速するが、**仮説を立てるのも、外れを認めるのも、次を決めるのも人間**。
> ここに、AI 時代の開発者(そして AX セールス)の介在価値がある。
